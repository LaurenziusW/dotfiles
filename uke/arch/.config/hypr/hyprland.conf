# =============================================================================
# UKE v8 - Hyprland Entry Point
# =============================================================================

# 1. Load Machine-Specific Profile (Monitors, etc.)
#    Managed by `uke-profile`. Defaults to `profiles/fallback.conf`.
source = ~/.config/hypr/machine.conf

# 2. Load Shared Configuration
source = ~/.config/hypr/common.conf

# 3. Load User Overrides (Optional)
#    Create this file to override anything without git tracking.
#    (Commented out to prevent globbing error if missing. Uncomment if you create it.)
# source = ~/.config/hypr/local.conf